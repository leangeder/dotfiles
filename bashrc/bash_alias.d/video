mplayer()
{
#set -x
	twidth=$(tmux display -p '#{pane_width}')
	theight=$(tmux display -p '#{pane_height}')
	ttop=$(tmux display -p '#{pane_top}')
	tleft=$(tmux display -p '#{pane_left}')

#	mplayer "$@" -vo fbdev2 -geometry ${twidth}x${theight}
}
